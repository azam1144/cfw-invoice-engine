module.exports = {
    script: './src/index.js', // Update this to point to your Worker entry file
    // If you're using TypeScript, you may want to compile it first or use `ts-node`
    // You could also enable any environment variables or configure databases
};